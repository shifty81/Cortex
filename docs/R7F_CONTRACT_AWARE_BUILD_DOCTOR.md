# R7F Contract-aware project Build Doctor
The doctor now reads the project's real `project.control.json`/native PCC discovery result and bounded nested build markers. Zero detected requirements is INCOMPLETE, never READY. It reports command authority, requirements, executable paths, versions, and blockers while using the portable volume environment.
